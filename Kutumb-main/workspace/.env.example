# This file is a template for the required environment variables.
# Copy this file to .env and fill in your actual Firebase project credentials.

# --- Firebase Admin SDK (Server-Side) ---
# Used for server actions, data fetching, and admin panel logic.
# You can get these values from your Firebase project's service account JSON file.
# Go to Firebase Console > Project Settings > Service accounts > Generate new private key.
FIREBASE_PROJECT_ID="your-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com"
# For the private key, copy the entire key, including "-----BEGIN PRIVATE KEY-----" and "-----END PRIVATE KEY-----".
# It's recommended to wrap the key in double quotes to handle special characters.
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# --- Firebase Client SDK (Client-Side) ---
# These are used for client-side authentication and real-time features.
# It is safe to expose these. Find them in Firebase Console > Project Settings > General > Your apps.
NEXT_PUBLIC_FIREBASE_API_KEY="your-api-key"
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="your-messaging-sender-id"
NEXT_PUBLIC_FIREBASE_APP_ID="your-app-id"
